# Pytorch learning environment

## Features

- Torch
- Jupyter lab
- GPU through CDI

## How to start

- Run the compose file

```shell
docker compose up
```

- Open the `localhost:8888/*` link in the console window from browser
